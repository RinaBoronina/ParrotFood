import React from 'react';
import './footer.css';
import Logo from '../Logo/Logo';

export const Footer = () => {
    return (
        <footer className="footer">
            <div className="container">
                <Logo />
            </div>
        </footer>
    );
};
